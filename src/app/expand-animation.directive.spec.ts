import { ExpandAnimationDirective } from './expand-animation.directive';

describe('ExpandAnimationDirective', () => {
  it('should create an instance', () => {
    const directive = new ExpandAnimationDirective();
    expect(directive).toBeTruthy();
  });
});
